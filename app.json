{
  "name": "WordChainBattleBot",
  "description": "Word Chain Battle – addictive Telegram group game bot with streaks, hard mode, leaderboard and MongoDB",
  "repository": "https://github.com/yourusername/word-chain-bot",
  "keywords": ["telegram", "game-bot", "word-chain", "leaderboard", "mongodb", "python"],
  "buildpacks": [
    { "url": "heroku/python" }
  ],
  "env": {
    "API_ID": {
      "description": "Telegram API ID from https://my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Telegram API HASH from https://my.telegram.org",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Telegram Bot Token from @BotFather",
      "required": true
    },
    "MONGO_URI": {
      "description": "MongoDB connection URI (MongoDB Atlas)",
      "required": true
    }
  },
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "Basic"
    }
  },
  "scripts": {
    "postdeploy": "echo '🔤🔥 Word Chain Battle Bot deployed successfully!'"
  }
}
